/*
1.data fetch
convert object to string by JSON.stringify
convert string-object to object by JSON.parse

2. keys, values
const keys = Object.keys(object);
const values = Object.values(object);

3. foreach, map
when need to return definitely use map
when no need to return definitely use for each


4. 
for of loop use  on array like object
for in loop use on object

5. add or remove on array 
add
e.g. newProducts = [...products, newProduct];

remove means create an array without specific one
e.g. remaining = products.filter(p => p.id !== 'phone');



*/